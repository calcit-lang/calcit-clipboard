# Calcit 0.15.3 语法检查 / Calcit 0.15.3 syntax checks

## 中文

- 将模块固定到 Calcit 0.15.3。
- 使用 `surface-latest-v2` 检查完整 Snapshot；当前源码没有待应用的语法迁移。

## English

- Pin the module to Calcit 0.15.3.
- Check the full Snapshot with `surface-latest-v2`; the current source has no pending syntax migrations.

## 验证 / Verification

- `calcit fix --preset surface-latest-v2 --format edn`
- `calcit --check-only --strict-types`
- `calcit analyze quality`
- `cargo fmt --all -- --check`
- `cargo test`
- `cargo clippy --all-targets -- -D warnings`
