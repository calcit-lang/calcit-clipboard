# Upgrade to Calcit 0.14.7

- Raise the required Calcit version to 0.14.7 and prepare module version 0.0.14.
- Enforce strict, zero-debt type and dynamic-method checks in CI.
- Check Markdown formatting before the native build and execute the deterministic copy example under `xvfb-run` after the clipboard dylib is available; paste remains explicitly host-state dependent.
- Retain the C-safe buffer FFI v1 boundary owned by `calcit_native_ffi`.
